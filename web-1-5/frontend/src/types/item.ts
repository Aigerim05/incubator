export type Item = {
    id: number;
    name: string;
    password: string;
    description: string;
  };
  
  export type ItemCreate = {
    name: string;
    password: string;
    description: string;
  };
  